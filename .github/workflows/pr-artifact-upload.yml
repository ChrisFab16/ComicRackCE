# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: Pull Request - Artifact Upload

on:
  pull_request:
    types:
      - opened
      - reopened
      - synchronize
    paths-ignore:
      - '**.md'

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v7
      with:
        submodules: recursive

    - name: setup-msbuild
      uses: microsoft/setup-msbuild@v3

    - name: Setup MSVC Dev Cmd
      uses: ilammy/msvc-dev-cmd@v1

    - name: Setup NuGet
      uses: NuGet/setup-nuget@v4

    - name: Restore Packages
      run: nuget restore ComicRack.sln

    - name: Build solution
      run: msbuild ComicRack\ComicRack.csproj -t:rebuild -property:Configuration=Release

    - name: Verify embedded PerMonitorV2 manifest
      run: powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-embedded-manifest.ps1 -ExePath ComicRack/bin/Release/net48/ComicRack.exe

    - name: Run HiDPI unit tests
      run: msbuild ComicRack.Tests\ComicRack.Tests.csproj /t:Restore,Build /property:Configuration=Release /property:LangVersion=13.0 && dotnet test ComicRack.Tests\ComicRack.Tests.csproj -c Release --no-build

    - name: Upload artifacts
      id: upload
      uses: actions/upload-artifact@v7
      with:
        name: "ComicRackCE_PR"
        path: "ComicRack/bin/Release/net48/*"
        if-no-files-found: error